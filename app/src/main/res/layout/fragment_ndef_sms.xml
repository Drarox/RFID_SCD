<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/NDEFSmsFragmentId"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NDEFSmsFragment" >

    <RelativeLayout
        android:id="@+id/rlsmsId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        
    <RelativeLayout
        android:id="@+id/rlcontactId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="10dp"
        android:orientation="horizontal" >
    
    	<TextView
        	android:id="@+id/contactTvId"
        	android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:text="Tel : "/>
        
		<EditText
		    android:id="@+id/contactEtId"
		    android:layout_width="match_parent"
		    android:layout_height="wrap_content"
		    android:layout_toRightOf="@id/contactTvId"
		    android:background="#cfe7f5"
		    android:gravity="top"
		    android:imeOptions="actionDone"
		    android:inputType="number"
		    android:maxLines="3"
		    android:textColor="@color/dark_gray" />
	</RelativeLayout>

	<RelativeLayout
	    android:id="@+id/rlsmsmsgId"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:layout_below="@id/rlcontactId"
	    android:orientation="horizontal" >
    
    	<TextView
        	android:id="@+id/smsmsgTvId"
        	android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:text="Message"/>
        
		<EditText
		    android:id="@+id/smsmsgEtId"
		    android:layout_width="match_parent"
		    android:layout_height="wrap_content"
		    android:layout_toRightOf="@id/smsmsgTvId"
		    android:background="#cfe7f5"
		    android:gravity="top"
		    android:imeOptions="actionDone"
		    android:inputType="textCapSentences|textAutoCorrect|textMultiLine"
		    android:lines="10"
		    android:maxLines="15"
		    android:minLines="10"
		    android:textColor="@color/dark_gray"/>

	</RelativeLayout>
    
        
    </RelativeLayout>
        
</FrameLayout>
